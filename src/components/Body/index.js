import GroupButton from "../GroupButton";
import ListCart from "../ListCart";

const Body = () => {
  return (
    <section>
      <GroupButton />
      <ListCart />
    </section>
  );
};

export default Body;
