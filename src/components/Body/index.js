import GroupButton from "../GroupButton";
import ListCart from "../ListCart";
import Select from "../Select";

const Body = () => {
  return (
    <section>
      <GroupButton />
      <Select />
      <ListCart />
    </section>
  );
};

export default Body;
