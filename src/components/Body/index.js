import GroupButton from "../GroupButton";
import ListCart from "../ListCart";
import SelectFrameword from "../Select";

const Body = () => {
  return (
    <section>
      <GroupButton />
      <SelectFrameword />
      <ListCart />
    </section>
  );
};

export default Body;
